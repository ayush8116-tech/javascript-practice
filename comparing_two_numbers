const a = 7;
const b = 10;

if (a < b) {
    console.log(a + " is smaller than " + b);
} else {
    console.log(b + " is smaller than " + a);
}
let c = a < b ? a + " is smaller than " + b : b + " is smaller than " + a ;
console.log(c);
    

