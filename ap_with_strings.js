const difference = "..."
let firstTerms = "...";
let term ="";
const lastTerm = ".............................."

while(term.length<lastTerm.length){

    console.log(firstTerms.length);
    firstTerms = firstTerms + difference;
     term = term + ".";
}


