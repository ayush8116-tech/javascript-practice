const firstNumber = 6;
const difference = 2;
const numOfTerms = 7;
let valueOfTerm ;
let countCheck = true;

for(let countFactor = 0 ; countFactor < numOfTerms ; countFactor++){

         valueOfTerm = firstNumber + (countFactor * difference);
        console.log(valueOfTerm);
}
